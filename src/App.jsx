import React from "react";
import ReactDOM from "react-dom/client";

import Layout from "./interface/components/main/Layout.jsx";

ReactDOM.createRoot(document.getElementById("app")).render(
    <React.StrictMode>
        <Layout />
    </React.StrictMode>
);
